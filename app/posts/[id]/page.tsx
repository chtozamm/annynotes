"use client";

import { Post } from "@/app/types";

export default function PostPage() {
//   {
//   params,
//   posts,
// }: {
//   params: { id: string };
//   posts: Post[];
// }
  return (
    <>
      {/* <ul>
        {posts.map((post) => (
          <li key={post.id}>{post.sender_name}</li>
        ))}
      </ul> */}
      <div>
        params.id:
        {/* {params.id} */}
      </div>
    </>
  );
}
